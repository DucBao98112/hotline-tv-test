<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="95dp"
    android:layout_marginEnd="-8dp"
    android:layout_marginBottom="-8dp"
    android:background="@drawable/bg_short_not_register"
    android:alpha="0.85"
    android:descendantFocusability = "afterDescendants"
    android:focusable="true"
    android:gravity="center_vertical"
    android:padding="@dimen/grid_item_space">

    <TextView
        android:id="@+id/tv_short"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/padding_small"
        android:ellipsize="end"
        android:maxLength="1"
        android:maxLines="1"
        android:textColor="?attr/text_color_light"
        android:textSize="25sp"
        android:alpha="0.75"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="1" />

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/padding_large"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="?attr/text_color_light"
        android:textSize="@dimen/font_card_text_normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/tv_short"
        app:layout_constraintTop_toTopOf="parent"
        android:text="@string/lbl_new_short"
        app:drawableTint="?attr/text_color_light"
        app:drawableStartCompat="@drawable/ic_action_add" />

</androidx.constraintlayout.widget.ConstraintLayout>